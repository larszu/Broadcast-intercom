/**
 * Config/action/feedback upgrade scripts.
 *
 * None are needed for the initial 1.0.0 release, but the array must exist and be
 * passed to `runEntrypoint` so future versions can migrate saved configs.
 */
export const upgradeScripts = []
