{
  "name": "@broadcast/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Broadcast Intercom desktop shell (Electron) — bundles the intercom core + web UI into a Mac/Windows app",
  "author": "Lars Zumpe",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --config electron-builder.js"
  },
  "devDependencies": {
    "electron": "33.4.11",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.2"
  },
  "license": "UNLICENSED"
}
