node_modules/
dist/
coverage/
*.log
apps/web/certs/

# electron-builder-Ausgabe (Installer, Manifeste, entpackte App). Entsteht im
# Release-Lauf neu und gehoert nicht ins Repo.
release/

# Sicherungskopien, die `atomicWrite` beim Speichern anlegt (configStore.ts).
# Sie gehoeren dem laufenden Geraet, nicht dem Repo.
data/configs/*.bak

# Die gebaute Projektseite (scripts/build-site.py). Sie entsteht in CI neu;
# eine eingecheckte Kopie waere ein zweiter Stand derselben Doku.
_site/
